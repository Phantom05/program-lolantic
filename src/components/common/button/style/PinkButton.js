import {PureButton} from 'components/common/button';
import styled from 'styled-components';
import {buttonPink} from 'styles/_utils';

const PinkButton = styled(PureButton)`
  ${buttonPink};
  padding:8px 20px;
  width:100%;
`

export default PinkButton;